import { createActionString } from '../utils/index';

export const UPDATE_PAGETOSHOW = createActionString('UPDATE_PAGETOSHOW', 'UI');
export const ON_NEXT_PAGE = createActionString('ON_NEXT_PAGE', 'UI');
export const ON_PREV_PAGE = createActionString('ON_PREV_PAGE', 'UI');